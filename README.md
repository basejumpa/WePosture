# WeBalance

**WIP - Work In Progress**

Just applied some minor modifications to the example in Getting Started at  https://pypi.org/project/hx711-multi/

```bash
poetry install
poetry run python -m webalance
```

```bash
source .venv/bin/activate
``´

```bash
python -m webalance --help
python -m webalance zero-device
python -m webalance calibrate-device 4.0
python -m webalance measure 10.0
```
